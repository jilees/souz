openapi: 3.1.0
info:
  title: Client-Souz REST API
  version: 1.0.0-draft
  description: REST operations and reusable frame schemas for single-chat and multi-chat WebSockets. The multi-chat endpoint is /v1/ws?clientType=backend; WebSocket upgrade routes are excluded from REST paths.
servers:
  - url: https://souz.example.ru
paths:
  /v1/chats:
    post:
      operationId: createClientChat
      tags: [Client Chats]
      summary: Create a chat
      description: Creates a user-owned Souz chat for either WebSocket endpoint. Shares creation idempotency with `chat.create` on `/v1/ws?clientType=backend`. The request is idempotent by `(userId, requestId)`, with `userId` supplied in the JSON body. This API is only exposed inside a trusted environment, so credentials are not required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateChatRequest"
      responses:
        "200":
          description: Existing chat creation result returned for an idempotent retry.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateChatResponse"
        "201":
          description: Chat created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateChatResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          $ref: "#/components/responses/Conflict"
  /v1/chats/{chatId}/threads/{threadId}:
    get:
      operationId: getClientThreadStatus
      tags: [Client Chats]
      summary: Get thread status
      description: Returns the current durable status for a Client-Souz thread. This API is only exposed inside a trusted environment, so credentials are not required.
      parameters:
        - name: chatId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/UUID"
        - name: threadId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/UUID"
        - name: clientType
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/ClientType"
      responses:
        "200":
          description: Current thread status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ThreadStatusSnapshot"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
components:
  responses:
    BadRequest:
      description: Invalid request headers, content type, or JSON body.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    Conflict:
      description: Request conflicts with idempotency state.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    NotFound:
      description: Chat or thread not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
  schemas:
    CreateChatRequest:
      type: object
      additionalProperties: false
      required: [userId, requestId, clientType]
      properties:
        userId:
          $ref: "#/components/schemas/UUID"
          description: Trusted user identity for chat ownership and create-chat idempotency scope.
        requestId:
          $ref: "#/components/schemas/NonEmptyString"
          description: Client-generated idempotency key for this chat creation attempt. This scope is independent from `message.submit.requestId`.
        clientType:
          $ref: "#/components/schemas/ClientType"
          description: Client kind for this chat.
        title:
          type: [string, "null"]
    CreateChatResponse:
      type: object
      additionalProperties: false
      required: [requestId, duplicate, chat]
      properties:
        requestId:
          $ref: "#/components/schemas/NonEmptyString"
        duplicate:
          type: boolean
          description: True when this response repeats a stored result for the same normalized create-chat payload.
        chat:
          $ref: "#/components/schemas/Chat"
    ClientType:
      type: string
      enum: [backend, mobile_app]
    Chat:
      type: object
      additionalProperties: false
      required: [id]
      properties:
        id:
          $ref: "#/components/schemas/UUID"
        title:
          type: [string, "null"]
    UUID:
      type: string
      format: uuid
    DateTime:
      type: string
      format: date-time
    NonEmptyString:
      type: string
      minLength: 1
      pattern: "\\S"
    ArbitraryJson:
      description: Any JSON value.
    WebSearchArguments:
      description: Documented arguments for the client web.search Skill. The generic transport does not validate this operation-specific schema.
      type: object
      additionalProperties: false
      required: [query]
      properties:
        query:
          $ref: "#/components/schemas/NonEmptyString"
          description: Search query containing at least one non-whitespace character.
    WebSearchResult:
      description: Normalized Search API evidence returned by the client proxy for web.search. The generic transport does not validate this operation-specific schema.
      type: object
      additionalProperties: false
      required: [documents]
      properties:
        documents:
          type: array
          description: Search evidence; an empty array is a successful search with no matches.
          items:
            $ref: "#/components/schemas/WebSearchDocument"
    WebSearchDocument:
      description: Source text excluding the plugin's system/user prompt messages. Preserve combined search text as one document when reliable source boundaries are unavailable.
      type: object
      additionalProperties: false
      required: [text]
      properties:
        text:
          type: string
        title:
          type: string
          description: Source title, only when provided by the search result.
        url:
          type: string
          format: uri
          description: Source URL, only when provided by the search result.
    DeviceType:
      type: string
      enum: [tv_box, smart_speaker, smartphone, unknown]
    DeviceCapability:
      type: string
      enum: [speech, screen, device_tools, user_permissions, deep_links, oauth]
    Device:
      type: object
      additionalProperties: false
      required: [userId, deviceId, deviceType, capabilities]
      properties:
        userId:
          $ref: "#/components/schemas/UUID"
          description: Trusted user identity for the device scope.
        deviceId:
          $ref: "#/components/schemas/NonEmptyString"
        deviceType:
          $ref: "#/components/schemas/DeviceType"
        capabilities:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/DeviceCapability"
        appVersion:
          type: string
        platform:
          type: string
    ClientMessage:
      description: Client-to-Souz frame. ChatCreate and ChatSubscribe are accepted only on /v1/ws?clientType=backend.
      oneOf:
        - $ref: "#/components/schemas/ChatCreate"
        - $ref: "#/components/schemas/ChatSubscribe"
        - $ref: "#/components/schemas/MessageSubmit"
        - $ref: "#/components/schemas/HistoryAppend"
        - $ref: "#/components/schemas/SucceededToolResult"
        - $ref: "#/components/schemas/FailedToolResult"
        - $ref: "#/components/schemas/CancelledToolResult"
        - $ref: "#/components/schemas/TimedOutToolResult"
        - $ref: "#/components/schemas/ThreadCancel"
    ChatCreate:
      description: Shares HTTP creation normalization and (userId, requestId) idempotency. Inherits clientType from the socket and automatically subscribes to live events after acknowledgement.
      type: object
      additionalProperties: false
      required: [kind, requestId, payload]
      properties:
        kind:
          type: string
          enum: [chat.create]
        requestId:
          $ref: "#/components/schemas/NonEmptyString"
        payload:
          type: object
          additionalProperties: false
          required: [userId]
          properties:
            userId:
              $ref: "#/components/schemas/UUID"
            title:
              type: [string, "null"]
    ChatSubscribe:
      description: Acknowledges before replay and live delivery. Explicit afterSeq replaces this chat subscription on the same socket and returns duplicate=false. Without afterSeq, an existing subscription is kept with duplicate=true. Does not execute input; requestId is for correlation only.
      type: object
      additionalProperties: false
      required: [kind, chatId, requestId]
      properties:
        kind:
          type: string
          enum: [chat.subscribe]
        chatId:
          $ref: "#/components/schemas/UUID"
        requestId:
          $ref: "#/components/schemas/NonEmptyString"
        afterSeq:
          description: Exclusive chat-local cursor. When present, restarts replay even for an existing subscription. When omitted, keeps an existing subscription or replays from zero for a new subscription.
          type: integer
          format: int64
          minimum: 0
          maximum: 9223372036854775807
    MessageSubmit:
      description: Submits user input and selects, continues, or creates an execution thread.
      type: object
      additionalProperties: false
      required: [kind, chatId, requestId, payload]
      properties:
        kind:
          type: string
          enum: [message.submit]
        chatId:
          $ref: "#/components/schemas/UUID"
        requestId:
          $ref: "#/components/schemas/NonEmptyString"
        threadId:
          description: Thread to continue. When absent or null, Souz continues the chat's active thread or creates one if none is active.
          anyOf:
            - $ref: "#/components/schemas/UUID"
            - type: "null"
        payload:
          $ref: "#/components/schemas/MessageSubmitPayload"
    HistoryAppend:
      description: Appends role-preserving chat history without selecting or mutating a thread or triggering model execution. Stored history is supplied with the next message.submit.
      type: object
      additionalProperties: false
      required: [kind, chatId, requestId, payload]
      properties:
        kind:
          type: string
          enum: [history.append]
        chatId:
          $ref: "#/components/schemas/UUID"
        requestId:
          $ref: "#/components/schemas/NonEmptyString"
        payload:
          $ref: "#/components/schemas/HistoryAppendPayload"
    MessageSubmitPayload:
      type: object
      additionalProperties: false
      required: [device, content]
      properties:
        device:
          $ref: "#/components/schemas/Device"
        content:
          $ref: "#/components/schemas/RecognizedTextContent"
        meta:
          $ref: "#/components/schemas/RequestMeta"
    HistoryAppendPayload:
      type: object
      additionalProperties: false
      required: [role, content]
      if:
        properties:
          content:
            properties:
              type:
                const: tool_call
      then:
        properties:
          role:
            const: assistant
      properties:
        role:
          description: Original message role.
          type: string
          enum: [user, assistant]
        content:
          oneOf:
            - $ref: "#/components/schemas/RecognizedTextContent"
            - $ref: "#/components/schemas/HistoryToolCallContent"
          discriminator:
            propertyName: type
            mapping:
              text: "#/components/schemas/RecognizedTextContent"
              tool_call: "#/components/schemas/HistoryToolCallContent"
    RecognizedTextContent:
      type: object
      additionalProperties: false
      required: [type, source, text]
      properties:
        type:
          type: string
          enum: [text]
        source:
          type: string
          enum: [voice, text]
        text:
          $ref: "#/components/schemas/NonEmptyString"
    HistoryToolCallContent:
      description: Completed client tool call with its result. Requires an assistant history role; name is the Skill ID passed to RunSkillCommand.
      type: object
      additionalProperties: false
      required: [type, name, arguments, result]
      properties:
        type:
          type: string
          enum: [tool_call]
        name:
          $ref: "#/components/schemas/NonEmptyString"
        arguments:
          type: object
        result:
          type: object
    RequestMeta:
      type: object
      additionalProperties: false
      properties:
        locale:
          type: string
        timeZone:
          type: string
        model:
          type: string
    SucceededToolResult:
      type: object
      additionalProperties: false
      required: [kind, chatId, threadId, toolCallId, status, result]
      properties:
        kind:
          type: string
          enum: [tool.result]
        chatId:
          $ref: "#/components/schemas/UUID"
        threadId:
          $ref: "#/components/schemas/UUID"
        toolCallId:
          $ref: "#/components/schemas/NonEmptyString"
        status:
          type: string
          enum: [succeeded]
        result:
          $ref: "#/components/schemas/ArbitraryJson"
          description: Operation result; web.search uses the documented WebSearchResult shape.
    FailedToolResult:
      type: object
      additionalProperties: false
      required: [kind, chatId, threadId, toolCallId, status, error]
      properties:
        kind:
          type: string
          enum: [tool.result]
        chatId:
          $ref: "#/components/schemas/UUID"
        threadId:
          $ref: "#/components/schemas/UUID"
        toolCallId:
          $ref: "#/components/schemas/NonEmptyString"
        status:
          type: string
          enum: [failed]
        error:
          $ref: "#/components/schemas/Error"
    CancelledToolResult:
      type: object
      additionalProperties: false
      required: [kind, chatId, threadId, toolCallId, status, error]
      properties:
        kind:
          type: string
          enum: [tool.result]
        chatId:
          $ref: "#/components/schemas/UUID"
        threadId:
          $ref: "#/components/schemas/UUID"
        toolCallId:
          $ref: "#/components/schemas/NonEmptyString"
        status:
          type: string
          enum: [cancelled]
        error:
          $ref: "#/components/schemas/Error"
    TimedOutToolResult:
      type: object
      additionalProperties: false
      required: [kind, chatId, threadId, toolCallId, status, error]
      properties:
        kind:
          type: string
          enum: [tool.result]
        chatId:
          $ref: "#/components/schemas/UUID"
        threadId:
          $ref: "#/components/schemas/UUID"
        toolCallId:
          $ref: "#/components/schemas/NonEmptyString"
        status:
          type: string
          enum: [timed_out]
        error:
          $ref: "#/components/schemas/Error"
    ThreadCancel:
      type: object
      additionalProperties: false
      required: [kind, chatId, requestId, threadId]
      properties:
        kind:
          type: string
          enum: [thread.cancel]
        chatId:
          $ref: "#/components/schemas/UUID"
        requestId:
          $ref: "#/components/schemas/NonEmptyString"
        threadId:
          $ref: "#/components/schemas/UUID"
        reason:
          type: string
          enum: [user_requested, superseded, device_disconnected]
    SouzFrame:
      description: Souz-to-Client WebSocket frame.
      oneOf:
        - $ref: "#/components/schemas/SouzAck"
        - $ref: "#/components/schemas/ThreadStatusFrame"
        - $ref: "#/components/schemas/SouzServerEvent"
    SouzAck:
      oneOf:
        - $ref: "#/components/schemas/AcceptedChatCreateAck"
        - $ref: "#/components/schemas/RejectedChatCreateAck"
        - $ref: "#/components/schemas/AcceptedChatSubscribeAck"
        - $ref: "#/components/schemas/RejectedChatSubscribeAck"
        - $ref: "#/components/schemas/AcceptedMessageSubmitAck"
        - $ref: "#/components/schemas/RejectedMessageSubmitAck"
        - $ref: "#/components/schemas/AcceptedHistoryAppendAck"
        - $ref: "#/components/schemas/RejectedHistoryAppendAck"
        - $ref: "#/components/schemas/AcceptedToolResultAck"
        - $ref: "#/components/schemas/RejectedToolResultAck"
        - $ref: "#/components/schemas/AcceptedThreadCancelAck"
        - $ref: "#/components/schemas/RejectedThreadCancelAck"
    AcceptedChatCreateAck:
      type: object
      additionalProperties: false
      required: [kind, type, userId, chatId, requestId, status, duplicate, error, receivedAt]
      properties:
        kind:
          type: string
          enum: [ack]
        type:
          type: string
          enum: [chat.create]
        userId:
          $ref: "#/components/schemas/UUID"
        chatId:
          $ref: "#/components/schemas/UUID"
        requestId:
          $ref: "#/components/schemas/NonEmptyString"
        status:
          type: string
          enum: [accepted]
        duplicate:
          type: boolean
        error:
          type: "null"
        receivedAt:
          $ref: "#/components/schemas/DateTime"
    RejectedChatCreateAck:
      type: object
      additionalProperties: false
      required: [kind, type, userId, chatId, requestId, status, duplicate, error, receivedAt]
      properties:
        kind:
          type: string
          enum: [ack]
        type:
          type: string
          enum: [chat.create]
        userId:
          type: [string, "null"]
        chatId:
          type: "null"
        requestId:
          type: string
        status:
          type: string
          enum: [rejected]
        duplicate:
          type: boolean
          const: false
        error:
          $ref: "#/components/schemas/Error"
        receivedAt:
          $ref: "#/components/schemas/DateTime"
    AcceptedChatSubscribeAck:
      type: object
      additionalProperties: false
      required: [kind, type, chatId, requestId, status, duplicate, error, receivedAt]
      properties:
        kind:
          type: string
          enum: [ack]
        type:
          type: string
          enum: [chat.subscribe]
        chatId:
          $ref: "#/components/schemas/UUID"
        requestId:
          $ref: "#/components/schemas/NonEmptyString"
        status:
          type: string
          enum: [accepted]
        duplicate:
          description: True only when afterSeq was omitted and the existing subscription was kept.
          type: boolean
        error:
          type: "null"
        receivedAt:
          $ref: "#/components/schemas/DateTime"
    RejectedChatSubscribeAck:
      type: object
      additionalProperties: false
      required: [kind, type, chatId, requestId, status, duplicate, error, receivedAt]
      properties:
        kind:
          type: string
          enum: [ack]
        type:
          type: string
          enum: [chat.subscribe]
        chatId:
          type: string
        requestId:
          type: string
        status:
          type: string
          enum: [rejected]
        duplicate:
          type: boolean
          const: false
        error:
          $ref: "#/components/schemas/Error"
        receivedAt:
          $ref: "#/components/schemas/DateTime"
    AcceptedMessageSubmitAck:
      type: object
      additionalProperties: false
      required: [kind, chatId, requestId, status, duplicate, thread, error, receivedAt]
      properties:
        kind:
          type: string
          enum: [ack]
        chatId:
          $ref: "#/components/schemas/UUID"
        requestId:
          $ref: "#/components/schemas/NonEmptyString"
        status:
          type: string
          enum: [accepted]
        duplicate:
          type: boolean
        thread:
          $ref: "#/components/schemas/AcknowledgedThread"
        error:
          type: "null"
        receivedAt:
          $ref: "#/components/schemas/DateTime"
    RejectedMessageSubmitAck:
      type: object
      additionalProperties: false
      required: [kind, chatId, requestId, status, duplicate, thread, error, receivedAt]
      properties:
        kind:
          type: string
          enum: [ack]
        chatId:
          type: string
        requestId:
          type: string
        status:
          type: string
          enum: [rejected]
        duplicate:
          type: boolean
        thread:
          type: "null"
        error:
          $ref: "#/components/schemas/Error"
        receivedAt:
          $ref: "#/components/schemas/DateTime"
    AcceptedHistoryAppendAck:
      description: History is durable chat context. This acknowledgement is not followed by thread status.
      type: object
      additionalProperties: false
      required: [kind, chatId, requestId, status, duplicate, error, receivedAt]
      properties:
        kind:
          type: string
          enum: [ack]
        chatId:
          $ref: "#/components/schemas/UUID"
        requestId:
          $ref: "#/components/schemas/NonEmptyString"
        status:
          type: string
          enum: [accepted]
        duplicate:
          type: boolean
        error:
          type: "null"
        receivedAt:
          $ref: "#/components/schemas/DateTime"
    RejectedHistoryAppendAck:
      type: object
      additionalProperties: false
      required: [kind, chatId, requestId, status, duplicate, error, receivedAt]
      properties:
        kind:
          type: string
          enum: [ack]
        chatId:
          type: string
        requestId:
          type: string
        status:
          type: string
          enum: [rejected]
        duplicate:
          type: boolean
        error:
          $ref: "#/components/schemas/Error"
        receivedAt:
          $ref: "#/components/schemas/DateTime"
    AcknowledgedThread:
      type: object
      additionalProperties: false
      required: [id, created, status]
      properties:
        id:
          $ref: "#/components/schemas/UUID"
        created:
          type: boolean
          description: True only when the originally acknowledged request created this thread.
        status:
          type: string
          enum: [running]
    AcceptedToolResultAck:
      type: object
      additionalProperties: false
      required: [kind, chatId, toolCallId, threadId, status, duplicate, error, receivedAt]
      properties:
        kind:
          type: string
          enum: [ack]
        chatId:
          $ref: "#/components/schemas/UUID"
        toolCallId:
          $ref: "#/components/schemas/NonEmptyString"
        threadId:
          $ref: "#/components/schemas/UUID"
        status:
          type: string
          enum: [accepted]
        duplicate:
          type: boolean
        error:
          type: "null"
        receivedAt:
          $ref: "#/components/schemas/DateTime"
    RejectedToolResultAck:
      type: object
      additionalProperties: false
      required: [kind, chatId, toolCallId, threadId, status, duplicate, error, receivedAt]
      properties:
        kind:
          type: string
          enum: [ack]
        chatId:
          type: string
        toolCallId:
          type: string
        threadId:
          type: string
        status:
          type: string
          enum: [rejected]
        duplicate:
          type: boolean
        error:
          $ref: "#/components/schemas/Error"
        receivedAt:
          $ref: "#/components/schemas/DateTime"
    AcceptedThreadCancelAck:
      type: object
      additionalProperties: false
      required: [kind, chatId, requestId, threadId, status, duplicate, error, receivedAt]
      properties:
        kind:
          type: string
          enum: [ack]
        chatId:
          $ref: "#/components/schemas/UUID"
        requestId:
          $ref: "#/components/schemas/NonEmptyString"
        threadId:
          $ref: "#/components/schemas/UUID"
        status:
          type: string
          enum: [accepted]
        duplicate:
          type: boolean
        error:
          type: "null"
        receivedAt:
          $ref: "#/components/schemas/DateTime"
    RejectedThreadCancelAck:
      type: object
      additionalProperties: false
      required: [kind, chatId, requestId, threadId, status, duplicate, error, receivedAt]
      properties:
        kind:
          type: string
          enum: [ack]
        chatId:
          type: string
        requestId:
          type: string
        threadId:
          type: string
        status:
          type: string
          enum: [rejected]
        duplicate:
          type: boolean
        error:
          $ref: "#/components/schemas/Error"
        receivedAt:
          $ref: "#/components/schemas/DateTime"
    ThreadStatus:
      type: string
      enum: [queued, running, waiting_option, cancelling, cancelled, completed, failed]
    ThreadStatusSnapshot:
      type: object
      additionalProperties: false
      required:
        - chatId
        - threadId
        - status
        - alive
        - acceptsInput
        - startedAt
        - finishedAt
        - runtimeLeaseExpiresAt
        - error
        - observedAt
      properties:
        chatId:
          $ref: "#/components/schemas/UUID"
        threadId:
          $ref: "#/components/schemas/UUID"
        status:
          $ref: "#/components/schemas/ThreadStatus"
        alive:
          type: boolean
          description: True when the thread is active and its runtime lease has not expired.
        acceptsInput:
          type: boolean
          description: True when the thread can accept `message.submit` follow-up input.
        startedAt:
          $ref: "#/components/schemas/DateTime"
        finishedAt:
          anyOf:
            - $ref: "#/components/schemas/DateTime"
            - type: "null"
        runtimeLeaseExpiresAt:
          anyOf:
            - $ref: "#/components/schemas/DateTime"
            - type: "null"
        error:
          anyOf:
            - $ref: "#/components/schemas/Error"
            - type: "null"
        observedAt:
          $ref: "#/components/schemas/DateTime"
    ThreadStatusFrame:
      type: object
      additionalProperties: false
      required:
        - kind
        - type
        - chatId
        - threadId
        - requestId
        - status
        - alive
        - acceptsInput
        - startedAt
        - finishedAt
        - runtimeLeaseExpiresAt
        - error
        - observedAt
      properties:
        kind:
          type: string
          enum: [status]
        type:
          type: string
          enum: [thread.status]
        chatId:
          $ref: "#/components/schemas/UUID"
        threadId:
          $ref: "#/components/schemas/UUID"
        requestId:
          type: [string, "null"]
        status:
          $ref: "#/components/schemas/ThreadStatus"
        alive:
          type: boolean
        acceptsInput:
          type: boolean
        startedAt:
          $ref: "#/components/schemas/DateTime"
        finishedAt:
          anyOf:
            - $ref: "#/components/schemas/DateTime"
            - type: "null"
        runtimeLeaseExpiresAt:
          anyOf:
            - $ref: "#/components/schemas/DateTime"
            - type: "null"
        error:
          anyOf:
            - $ref: "#/components/schemas/Error"
            - type: "null"
        observedAt:
          $ref: "#/components/schemas/DateTime"
    SouzServerEvent:
      oneOf:
        - $ref: "#/components/schemas/ToolCallStartedEvent"
        - $ref: "#/components/schemas/ThreadCompletedEvent"
        - $ref: "#/components/schemas/ThreadFailedEvent"
        - $ref: "#/components/schemas/ThreadCancelledEvent"
        - $ref: "#/components/schemas/MessageCreatedEvent"
    EventSeq:
      type: integer
      format: int64
      minimum: 1
    ToolCallStartedEvent:
      type: object
      additionalProperties: false
      required: [kind, seq, type, chatId, threadId, payload, createdAt]
      properties:
        kind:
          type: string
          enum: [event]
        seq:
          $ref: "#/components/schemas/EventSeq"
        type:
          type: string
          enum: [tool.call.started]
        chatId:
          $ref: "#/components/schemas/UUID"
        threadId:
          $ref: "#/components/schemas/UUID"
        payload:
          $ref: "#/components/schemas/ToolCallStartedPayload"
        createdAt:
          $ref: "#/components/schemas/DateTime"
    ToolCallStartedPayload:
      type: object
      additionalProperties: false
      required: [toolCallId, name, arguments]
      properties:
        toolCallId:
          $ref: "#/components/schemas/NonEmptyString"
        name:
          $ref: "#/components/schemas/NonEmptyString"
        deviceId:
          type: [string, "null"]
        arguments:
          $ref: "#/components/schemas/ArbitraryJson"
          description: Operation arguments; web.search uses the documented WebSearchArguments shape.
        deadlineAt:
          anyOf:
            - $ref: "#/components/schemas/DateTime"
            - type: "null"
    ThreadCompletedEvent:
      type: object
      additionalProperties: false
      required: [kind, seq, type, chatId, threadId, payload, createdAt]
      properties:
        kind:
          type: string
          enum: [event]
        seq:
          $ref: "#/components/schemas/EventSeq"
        type:
          type: string
          enum: [thread.completed]
        chatId:
          $ref: "#/components/schemas/UUID"
        threadId:
          $ref: "#/components/schemas/UUID"
        payload:
          $ref: "#/components/schemas/ThreadCompletedPayload"
        createdAt:
          $ref: "#/components/schemas/DateTime"
    ThreadCompletedPayload:
      type: object
      additionalProperties: false
      required: [response]
      properties:
        response:
          type: string
    ThreadFailedEvent:
      type: object
      additionalProperties: false
      required: [kind, seq, type, chatId, threadId, payload, createdAt]
      properties:
        kind:
          type: string
          enum: [event]
        seq:
          $ref: "#/components/schemas/EventSeq"
        type:
          type: string
          enum: [thread.failed]
        chatId:
          $ref: "#/components/schemas/UUID"
        threadId:
          $ref: "#/components/schemas/UUID"
        payload:
          $ref: "#/components/schemas/ThreadFailedPayload"
        createdAt:
          $ref: "#/components/schemas/DateTime"
    ThreadFailedPayload:
      type: object
      additionalProperties: false
      required: [error]
      properties:
        error:
          $ref: "#/components/schemas/Error"
    ThreadCancelledEvent:
      type: object
      additionalProperties: false
      required: [kind, seq, type, chatId, threadId, payload, createdAt]
      properties:
        kind:
          type: string
          enum: [event]
        seq:
          $ref: "#/components/schemas/EventSeq"
        type:
          type: string
          enum: [thread.cancelled]
        chatId:
          $ref: "#/components/schemas/UUID"
        threadId:
          $ref: "#/components/schemas/UUID"
        payload:
          $ref: "#/components/schemas/ThreadCancelledPayload"
        createdAt:
          $ref: "#/components/schemas/DateTime"
    ThreadCancelledPayload:
      type: object
      additionalProperties: false
      properties:
        reason:
          type: string
    MessageCreatedEvent:
      description: >-
        Out-of-band push into this chat's history that did not originate from a thread this client
        started (e.g. a message forwarded here from another of the user's channels). threadId is
        null in that case; ordinary in-thread messages are not delivered on this stream.
      type: object
      additionalProperties: false
      required: [kind, seq, type, chatId, threadId, payload, createdAt]
      properties:
        kind:
          type: string
          enum: [event]
        seq:
          $ref: "#/components/schemas/EventSeq"
        type:
          type: string
          enum: [message.created]
        chatId:
          $ref: "#/components/schemas/UUID"
        threadId:
          anyOf:
            - $ref: "#/components/schemas/UUID"
            - type: "null"
        payload:
          $ref: "#/components/schemas/MessageCreatedPayload"
        createdAt:
          $ref: "#/components/schemas/DateTime"
    MessageCreatedPayload:
      type: object
      additionalProperties: false
      required: [messageId, seq, role, content]
      properties:
        messageId:
          $ref: "#/components/schemas/UUID"
        seq:
          $ref: "#/components/schemas/EventSeq"
        role:
          type: string
        content:
          type: string
        clientMessageId:
          type: [string, "null"]
    ErrorCode:
      type: string
      enum:
        - invalid_request
        - chat_not_found
        - thread_not_found
        - thread_already_terminal
        - tool_call_not_found
        - idempotency_conflict
        - feature_disabled
        - message_rejected
        - client_tool_timed_out
        - internal_error
        # Reported by the client inside a tool.result's error — not produced by the backend.
        - web_search_failed
        - device_not_connected
        - device_disconnected
        - device_timeout
        - mcp_discovery_failed
        - mcp_target_not_found
        - mcp_server_unreachable
        - mcp_protocol_error
        - mcp_tool_not_found
        - mcp_invalid_arguments
    Error:
      type: object
      additionalProperties: false
      required: [code, message]
      properties:
        code:
          $ref: "#/components/schemas/ErrorCode"
        message:
          type: string
        details:
          type: object
          additionalProperties: true
    ErrorEnvelope:
      type: object
      additionalProperties: false
      required: [error]
      properties:
        error:
          $ref: "#/components/schemas/Error"
