# Drop into /etc/nginx/conf.d/ (http context, loaded before server blocks).
# Lets the same proxy_pass upgrade to a WebSocket for /v1/chats/{id}/ws
# while staying a normal keep-alive connection for everything else.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
